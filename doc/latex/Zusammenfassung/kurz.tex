\documentclass[12pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage[ngerman]{babel}
\usepackage{amsmath}
\usepackage{amsfonts}
\usepackage{amssymb}
\usepackage{amsthm}
\usepackage{graphicx}
\usepackage{geometry}
\usepackage{booktabs}
\usepackage{array}
\usepackage{enumitem}
\usepackage{csquotes}

\geometry{margin=2.5cm}

\newtheorem{theorem}{Theorem}[section]
\newtheorem{axiom}{Axiom}[section]
\newtheorem{definition}{Definition}[section]
\newtheorem{remark}{Bemerkung}[section]
\newtheorem{satz}{Satz}[section]
\newtheorem{lemma}{Lemma}[section]
\newtheorem{corollary}{Korollar}[section]

\title{Die vollständige WDBT+\\
\large{Weber-de-Broglie-Bohm-Theorie mit fraktalem Raum,\\
Dynamische Schwere-Trägheits-Theorie (DSTT),\\
$\Omega$-Theorie der Gravitation, fraktale Maxwell-Gleichungen,\\
BEC-Objekte, Neutrino-Theorie und fraktale Entropie}}
\author{Michael Czybor}
\date{März--April 2026}

\begin{document}

\maketitle

\begin{abstract}
Die Weber-de-Broglie-Bohm-Theorie mit fraktalem Raum (WDBT+) ist die fundamentale, deterministische, feldlose Teilchentheorie, aus der alle bekannten physikalischen Theorien als
emergente Grenzfälle hervorgehen. Sie basiert auf drei Säulen: (1) der Weber-Gravitation als geschwindigkeits- und beschleunigungsabhängiger Teilchenwechselwirkung, (2) dem
de Broglie-Bohm-Quantenpotential als singularitätsverhinderndem Führungsfeld und (3) einer fraktalen Raumdimension $D \approx 2,71$, die natürliche Cutoffs liefert und die
Feinstrukturkonstante erklärt. Aus dieser fundamentalen Theorie emergieren:

\begin{itemize}
\item Die Dynamische Schwere-Trägheits-Theorie (DSTT), die Ursache (Schwere) und Wirkung (Trägheit) trennt und mit der Zustandsvariablen $\beta(t)$ eine intrinsische Irreversibilität ($\dot{\beta} > 0$) einführt, die zu Spiralbahnen und einem kosmologischen Zeitpfeil führt.
\item Die $\Omega$-Theorie als nicht-lokale, instantane Feldtheorie der Gravitation, die Gravitation als fundamentales Rotationsfeld $\vec{\Omega}$ beschreibt und die Maxwell-Form der Gravitation emergieren lässt.
\item Fraktale Maxwell-Gleichungen für Elektrodynamik und Gravitation, die die Feinstrukturkonstante erklären und Renormierung überflüssig machen.
\item Bose-Einstein-Kondensat (BEC)-Objekte als singularitätsfreie Alternative zu Schwarzen Löchern mit einer maximalen Kernmasse von $M_{\text{BEC}} \approx 3 \times 10^6 M_{\odot}$.
\item Die Neutrino-Theorie, die das Neutrino als reines Quantenpotential-Teilchen (Q-Teilchen) identifiziert, seine Masse aus der fundamentalen Länge $l_0$ herleitet ($m_\nu \approx 0,1\,\text{eV}/c^2$) und die Nichtlokalität der Quantenmechanik physikalisch vermittelt.
\item Die Theorie der fraktalen Entropie, die einen geschlossenen, stationären Kreislauf der fraktalen Entropie beschreibt und den Wärmetod verhindert.
\end{itemize}

Die Theorie führt zu einem stationären, nicht-expandierenden Universum ohne Urknall, Dunkle Materie und Dunkle Energie. Sie ist mit allen bisherigen Beobachtungen verträglich und macht spezifische, testbare Vorhersagen für zukünftige Experimente.
\end{abstract}

\tableofcontents

\newpage

\section{Einleitung: Vom Teilchen zum Feld}

Die WDBT+ beschreibt die Gravitation als direkte, instantane Teilchen-Teilchen-\\Wechselwirkung nach dem Weber-Gesetz, gesteuert durch das Bohmsche Quantenpotential. Sie ist
fundamental feldlos. Die DSTT übersetzt diese Dynamik in die Sprache der Energieaufteilung zwischen radialer und tangentialer Komponente, eingefangen in der Zustandsvariablen
$\beta(t)$ mit der fundamentalen Eigenschaft $\dot{\beta} > 0$.

Die $\Omega$-Theorie ist der nächste Schritt der Emergenz. Sie ist der Kontinuumslimes der DSTT und damit die effektive Feldtheorie für makroskopische Systeme. An die Stelle der
direkten Teilchen-Wechselwirkung treten Felder, die die kollektive Wirkung vieler Massenpunkte beschreiben. Das zentrale neue Element ist das Vektorfeld $\vec{\Omega}$, das die
lokale Rotation des Raumes selbst repräsentiert.

Die $\Omega$-Theorie verhält sich zur ART wie die Maxwell-Theorie zur Newton-Mechanik, mit dem entscheidenden Unterschied, dass sie die Nicht-Lokalität und Irreversibilität ihrer
fundamentalen Basis bewahrt.

\section{Grundlagen der WDBT+}

\subsection{Die Weber-Elektrodynamik (WED)}

Die Weber-Elektrodynamik wurde von Wilhelm Weber (1846) als geschwindigkeits- und beschleunigungsabhängige Kraft formuliert:

\begin{equation}
F_{\text{WED}} = \frac{q_1 q_2}{4 \pi \epsilon_0 r^2} \left[ 1 - \frac{\dot{r}^2}{c^2} + 2 \frac{r \ddot{r}}{c^2} \right]
\end{equation}

In vektorieller Form, die alle Winkelabhängigkeiten korrekt erfasst:

\begin{equation}
\vec{F}_{\text{WED}} = \frac{q_1 q_2}{4\pi\epsilon_0 r^2} \left\{ \left[ 1 - \frac{\dot{r}^2}{c^2} + 2 \frac{r\ddot{r}}{c^2} \right] \hat{r} + \frac{2(\dot{r} \cdot \vec{v})}{c^2} \vec{v} \right\}
\end{equation}

Dabei ist $\beta_{\text{WED}} = 2$ fest in der Elektrodynamik. Diese Kraft erfüllt actio = reactio und erhält den Impuls, im Gegensatz zur Maxwell-Theorie, die dies nur unter bestimmten Bedingungen tut.

\subsection{Die Weber-Gravitation (WG)}

Tisserand (1882) übertrug den Weber-Ansatz auf die Gravitation. Die vollständige Weber-Gravitation in vektorieller Form lautet:

\begin{equation}
\vec{F}_{\text{WG}} = -\frac{GMm}{r^2} \left\{ \left[ 1 - \frac{\dot{r}^2}{c^2} + \beta_{\text{WG}} \frac{r \ddot{r}}{c^2} \right] \hat{r} + \frac{2(\dot{r} \cdot \vec{v})}{c^2} \vec{v} \right\}
\end{equation}

Für Massen gilt $\beta_{\text{WG}} = 0,5$, was die Periheldrehung des Merkur exakt reproduziert. Für Photonen gilt $\beta_{\text{WG}} = 1,0$.

Der zweite Term in Gleichung (3) -- der geschwindigkeitsabhängige Anteil -- ist nicht-zentral, geschwindigkeitsabhängig und besitzt eine nichtverschwindende Rotation. Damit enthält er bereits die Keimzelle dessen, was in der Maxwell-Gravitation als gravito-magnetisches Feld erscheint.

\subsection{Die Wirbelstruktur der WG}

Der für die Wirbelstruktur entscheidende Term in Gleichung (3) ist:

\begin{equation}
\vec{F}_{\text{tan}} = -\frac{GMm}{r^2} \cdot \frac{2(\dot{r} \cdot \vec{v})}{c^2} \vec{v}
\end{equation}

Dieser Term hat folgende Eigenschaften:
\begin{itemize}
\item Er ist nicht-zentral: seine Richtung hängt von $\vec{v}$ ab, nicht nur von $\vec{r}$
\item Er ist geschwindigkeitsabhängig (linear in $\vec{v}$ für jede Komponente)
\item Er besitzt eine nichtverschwindende Rotation (Wirbelstärke)
\item Er ist analog zum geschwindigkeitsabhängigen Term in der Weber-Elektrodynamik
\end{itemize}

Definiere die Wirbeldichte (Rotation) des Kraftfeldes:

\begin{equation}
\vec{\omega} = \nabla \times \vec{F}_{\text{WG}} = \nabla \times (\vec{F}_{\text{rad}} + \vec{F}_{\text{tan}})
\end{equation}

Der radiale Anteil $\vec{F}_{\text{rad}}$ ist ein Zentralfeld und hat keine Rotation ($\nabla \times \vec{F}_{\text{rad}} = 0$). Die gesamte Wirbelstärke kommt vom tangentialen Anteil:

\begin{equation}
\vec{\omega} = \nabla \times \vec{F}_{\text{tan}} = -\frac{2GMm}{c^2} \nabla \times \left( \frac{(\dot{r} \cdot \vec{v}) \vec{v}}{r^2} \right)
\end{equation}

Nach Ausführung der Rotation ergibt sich:

\begin{equation}
\vec{\omega} = \frac{6GMm}{c^2r^3} (\hat{r} \cdot \vec{v})(\hat{r} \times \vec{v})
\end{equation}

Dies ist die fundamentale Wirbelstruktur der WG. Sie hat folgende Eigenschaften:
\begin{itemize}
\item $\vec{\omega}$ ist proportional zu $1/r^3$ -- wie ein Dipolfeld
\item $\vec{\omega}$ ist senkrecht zu $\vec{r}$ und $\vec{v}$ (wegen $\hat{r} \times \vec{v}$)
\item $\vec{\omega}$ verschwindet, wenn $\vec{v} \parallel \hat{r}$ (rein radiale Bewegung) oder $\vec{v} \perp \hat{r}$ (reine Tangentialbewegung mit $\hat{r} \cdot \vec{v} = 0$)
\item Maximale Wirbelstärke bei $\hat{r} \cdot \vec{v} = \pm |\vec{v}|/\sqrt{2}$ ($45^\circ$-Bahnneigung)
\end{itemize}

\subsection{Das de Broglie-Bohm-Quantenpotential (DBT)}

Die de Broglie-Bohm-Theorie führt ein Quantenpotential ein, das die Teilchen deterministisch führt:

\begin{equation}
Q = -\frac{\hbar^2}{2m} \frac{\nabla^2 R}{R}
\end{equation}

Die vollständige Bewegungsgleichung lautet:

\begin{equation}
m\ddot{\vec{x}} = -\nabla V - \nabla Q
\end{equation}

Das Quantenpotential hat folgende entscheidende Eigenschaften:
\begin{itemize}
\item Es verhindert Singularitäten, da es bei hohen Dichten repulsiv wirkt
\item Es ermöglicht Materieentstehung aus dem Vakuum
\item Es führt zu deterministischen, verborgenen Variablen -- es gibt keinen Kollaps der Wellenfunktion
\item Im klassischen Grenzfall ($\hbar \rightarrow 0$) verschwindet es
\end{itemize}

\subsection{Die fraktale Raumdimension}

Der fundamentale Raum ist nicht glatt mit Dimension 3, sondern fraktal mit der Dimension:

\begin{equation}
D = \frac{\ln 20}{\ln(2 + \phi)} \approx 2.71, \quad \phi = \frac{1 + \sqrt{5}}{2}
\end{equation}

Diese Dimension ergibt sich aus einer optimalen Packungsstruktur (Dodekaeder) und ist eine Konsequenz der fundamentalen Geometrie, kein freier Parameter.

\begin{remark}[Gültigkeitsbereich der fraktalen Dimension]
Die fundamentale fraktale Dimension $D \approx 2,71$ gilt \textbf{nur unterhalb der fundamentalen Länge} $l_0 \approx 1,8 \times 10^{-15}$ m. Oberhalb dieser Skala erscheint der Raum durch Renormierungseffekte effektiv glatt mit $D_{\text{eff}} = 3$. Dies erklärt, warum das Newton'sche $1/r^2$-Gesetz auf allen getesteten Skalen (von $10^{-5}$ m bis zu astronomischen Distanzen) mit hoher Präzision gilt.

Die fraktale Mikrostruktur des Raumes ist auf makroskopischen Skalen nicht direkt messbar. Sie hinterlässt jedoch \textbf{indirekte Signaturen} in Form von Skalengesetzen mit fraktalen Exponenten (siehe Abschnitt 8.8).
\end{remark}

Aus dieser Dimension folgt die fundamentale Längenskala:

\begin{equation}
l_0 = \left( \frac{V_{\text{Dodekaeder}}}{V_{\text{Einheitskugel}}} \right)^{1/3} \lambda_p \approx 1,8 \times 10^{-15} \text{ m}
\end{equation}

wobei $\lambda_p$ die Compton-Wellenlänge des Protons ist. Diese Länge ist die kleinste physikalisch mögliche Distanz -- nichts kann kleiner sein. Sie ersetzt die Planck-Skala als natürliche untere Grenze.

\section{Dynamische Schwere-Trägheits-Theorie (DSTT)}

\subsection{Axiome der DSTT}

\begin{axiom}[Trennung von Ursache und Wirkung]
Schwere (Ursache) und Trägheit (Wirkung) sind fundamental verschieden. Die Schwere wirkt radial zwischen zwei Körpern. Es gibt keine zusätzlichen Felder, keine Raumzeitkrümmung -- nur diese eine radiale Wechselwirkung.
\end{axiom}

\begin{axiom}[Anisotrope Trägheit]
Die Trägheit zerfällt in zwei Komponenten: eine radiale und eine tangentiale Antwort. Die Schwerewirkung teilt sich auf in einen radialen Anteil $(1-\beta)F_G$ und einen tangentialen Anteil $\beta F_G$.
\end{axiom}

\begin{axiom}[Dynamische Aufteilung]
Ein Zustandsparameter $\beta(t) \in [0,1]$ bestimmt das Verhältnis. Er ist definiert als das Verhältnis von tangentialer zu Gesamtenergie:
\begin{equation}
\beta(t) = \frac{E_B}{E} = \frac{\frac{1}{2}mr^2\dot{\phi}^2}{\frac{1}{2}m\dot{r}^2 + \frac{1}{2}mr^2\dot{\phi}^2}
\end{equation}
\end{axiom}

\begin{axiom}[Monotonie]
Die tangentiale Komponente nimmt ständig zu:
\begin{equation}
\dot{\beta}(t) > 0
\end{equation}
Diese Monotonie ist eine direkte Konsequenz der Energieflüsse in der Weber-Gravitation.
\end{axiom}

\subsection{Bahnformen und Spiralbahnen}

Aus den Axiomen der DSTT folgt durch Eliminierung der Zeit:

\begin{equation}
\frac{dr}{d\phi} = \frac{r}{B}, \quad \text{mit } B = \frac{2E}{F_G}
\end{equation}

Die Lösung ist eine logarithmische Spirale:

\begin{equation}
r(\phi) = Ke^{\phi/B}
\end{equation}

Kreisbahnen und Ellipsen -- die Grundbausteine der newtonschen Mechanik und der ART -- existieren nicht. Sie sind Näherungen für kurze Zeiträume, in denen die Drift vernachlässigbar erscheint.

Für das Zweikörperproblem (Zentralmasse $M$, Testmasse $m$) liefert die DSTT eine Bewegungsgleichung, die auf eine logarithmische Spirale führt. Mit dem Ansatz $\Omega_\theta = k/r^2$ (der sich aus den Feldgleichungen ergibt) folgt aus den Gleichungen in Polarkoordinaten:

\begin{align}
\ddot{r} - r\dot{\phi}^2 &= -\frac{GM}{r^2} + 2r\dot{\phi}\Omega_\theta \\
r\ddot{\phi} + 2\dot{r}\dot{\phi} &= -2r\Omega_\theta
\end{align}

\subsection{Die Zustandsvariable $\beta$ in der DSTT}

In der $\Omega$-Theorie lässt sich die DSTT-Zustandsvariable $\beta$ direkt als lokales Verhältnis der Kraftkomponenten definieren:

\begin{equation}
\beta(\vec{r}, t) = \frac{|\vec{v} \times \vec{\Omega}|}{|\nabla \Phi| + |\vec{v} \times \vec{\Omega}|}
\end{equation}

Aus den Feldgleichungen und der Bewegungsgleichung folgt die für die DSTT fundamentale Eigenschaft:

\begin{equation}
\dot{\beta} > 0
\end{equation}

Dies ist eine Konsequenz der nicht-lokalen, instantanen Rückkopplungsdynamik der Theorie.

\subsection{Exzentrizitätsänderung}

Aus der Definition von $\beta$ und den Bahngleichungen folgt ein Zusammenhang mit der Exzentrizität $e$. Für eine Kepler-Ellipse gilt im Mittel:

\begin{equation}
\langle \beta_{\text{DSTT}} \rangle = \sqrt{1 - e^2}
\end{equation}

Dies folgt aus dem Virialsatz und der Mittelung von $1/r^2$ über einen Umlauf.

Aus $\dot{\beta}_{\text{DSTT}} > 0$ folgt dann zwingend:

\begin{equation}
\frac{d}{dt} \sqrt{1 - e^2} > 0 \quad \Rightarrow \quad \frac{de}{dt} < 0
\end{equation}

Die Exzentrizität nimmt langsam ab -- jede Bahn wird mit der Zeit kreisförmiger.

Die Größenordnung dieser Änderung ergibt sich für kleine Exzentrizitäten:

\begin{equation}
\dot{e} \approx -\frac{1}{2} \frac{GM}{c^2 a^2} \sqrt{\frac{GM}{a^3}} \cdot e
\end{equation}

Für den Mond ($a = 3,84 \times 10^8$ m, $e = 0,0549$, $M = M_{\text{Erde}}$) folgt:

\begin{equation}
\dot{e} \approx -2,7 \times 10^{-11} \text{ pro Jahr}
\end{equation}

Dieser Wert ist mit den heutigen Messmethoden (Lunar Laser Ranging) nicht nachweisbar. Die DSTT ist daher mit allen Beobachtungen verträglich.

\subsection{Perfekte Kreisbahnen sind unmöglich}

Aus $\dot{\beta} > 0$ folgt eine weitere fundamentale Konsequenz: Perfekte Kreisbahnen ($\beta = 1$, $e = 0$) werden nie erreicht.

Begründung:
\begin{itemize}
\item $\beta = 1$ würde $\dot{r} = 0$ für alle Zeiten erfordern.
\item Dann wäre $\dot{\beta} = 0$ (aus der Bewegungsgleichung), im Widerspruch zu $\dot{\beta} > 0$.
\item $\beta$ nähert sich dem Wert $1$ nur asymptotisch, erreicht ihn aber nie.
\end{itemize}

Dies bedeutet: Alle Bahnen im Universum sind prinzipiell offene Spiralen -- es gibt keine geschlossenen Ellipsen. Die Abweichung ist winzig, aber prinzipiell vorhanden.

\subsection{Gravitationsentropie und intrinsischer Zeitpfeil}

Die Monotonie $\dot{\beta} > 0$ führt zu einem weiteren grundlegenden Konzept: Die Gravitation selbst produziert Entropie.

In der klassischen Mechanik und in der Allgemeinen Relativitätstheorie sind die Bewegungsgleichungen zeitumkehrinvariant -- es gibt keinen intrinsischen Zeitpfeil. Die DSTT bricht diese Symmetrie:

\begin{equation}
\dot{\beta} > 0 \implies \text{Die Zeit hat eine Richtung}
\end{equation}

Dies erlaubt die Definition einer Gravitationsentropie:

\begin{equation}
S_{\text{Grav}} \sim -\beta \ln \beta - (1 - \beta) \ln(1 - \beta)
\end{equation}

Für diese Entropie gilt:

\begin{equation}
\dot{S}_{\text{Grav}} > 0
\end{equation}

Die Gravitation gehorcht also einem zweiten Hauptsatz -- sie ist ein irreversibler Prozess. Dies liefert einen intrinsischen kosmologischen Zeitpfeil, der unabhängig von der Materie existiert.

\subsection{Langfristige Drift}

Da $\beta(t)$ monoton wächst, wird jede anfängliche Einwärtsbewegung ($\dot{r} < 0$) in eine Auswärtsbewegung ($\dot{r} > 0$) umgekehrt. Der langfristige Trend ist eindeutig: Alle Satelliten, Planeten, Monde -- alles spiralisiert langfristig nach außen.

\section{Die $\Omega$-Theorie der Gravitation}

\subsection{Axiome der $\Omega$-Theorie}

\begin{axiom}[Nicht-Lokalität und Instantaneität]
Die Gravitationswirkung ist nicht-lokal und instantan. Es gibt keine Ausbreitung mit endlicher Geschwindigkeit; die Wechselwirkung ist simultan. Dieses Axiom ist direkte Konsequenz
der feldlosen Weber-\\Wechselwirkung der WDBT+.
\end{axiom}

\begin{remark}[Emergente Retardierung]
Die instantane Wirkung der Weber-\\Gravitation ist fundamental. Auf Skalen oberhalb von $l_0$ ist sie effektiv nicht nachweisbar, da die fraktale Geometrie eine Retardierung mit $c$
emergieren lässt. Alle bisherigen Messungen der Gravitationswellengeschwindigkeit (einschließlich GW170817) liegen im effektiven Bereich $r \gg l_0$ und sind daher mit der
instantanen fundamentalen Theorie verträglich. Die emergente Wellengleichung reproduziert die ART auf großen Skalen.
\end{remark}

\begin{axiom}[Das Rotationsfeld]
Die Gravitation wird durch ein fundamentales, axiales Vektorfeld $\vec{\Omega}(\vec{r}, t)$ beschrieben. Dieses Feld kodiert die lokale Rotationsdichte des Raumes und ist die Ursache für alle geschwindigkeitsabhängigen (tangentialen) Trägheitseffekte.
\end{axiom}

\begin{axiom}[Aufspaltung von Ursache und Wirkung]
Die Schwere (Ursache) und die Trägheit (Wirkung) sind fundamental verschieden. Die radiale Anziehung wird durch ein skalares Potential $\Phi(\vec{r}, t)$ beschrieben, die tangentialen Trägheitskräfte durch das Rotationsfeld $\vec{\Omega}(\vec{r}, t)$.
\end{axiom}

\begin{axiom}[DSTT-Kompatibilität]
Die Theorie ist so konstruiert, dass aus ihr die Existenz einer Zustandsvariablen $\beta(\vec{r}, t)$ folgt, die das lokale Verhältnis von tangentialer zu gesamter Trägheit angibt, und für die $\dot{\beta} > 0$ gilt. Dies verleiht der Gravitation einen intrinsischen Zeitpfeil.
\end{axiom}

\subsection{Definition der Felder}

\begin{definition}[Skalares Potential]
Das Potential der radialen Anziehung (gravito-elektrisches Potential) ist durch die Massendichte $\rho$ bestimmt:
\begin{equation}
\Phi(\vec{r}, t) = -G \int_{\vec{r} \neq \vec{r}'} d^3 r' \frac{\rho(\vec{r}', t)}{|\vec{r}' - \vec{r}|}
\end{equation}
Der Ausschluss der Selbstwechselwirkung ($\vec{r} \neq \vec{r}'$) ist essentiell für die Konsistenz der Theorie und vermeidet Divergenzen.
\end{definition}

\begin{definition}[Vektorpotential]
Das Vektorpotential, aus dem die Rotation abgeleitet wird, wird durch die Massenstromdichte $\vec{j}(\vec{r}, t) = \rho(\vec{r}, t)\vec{v}(\vec{r}, t)$ erzeugt:
\begin{equation}
\vec{A}(\vec{r}, t) = \frac{G}{c^2} \int_{\vec{r} \neq \vec{r}'} d^3 r' \frac{\vec{j}(\vec{r}', t)}{|\vec{r}' - \vec{r}|}
\end{equation}
\end{definition}

\begin{definition}[Rotationsfeld]
Das fundamentale Rotationsfeld $\vec{\Omega}$ ist die Wirbelstärke des Vektorpotentials:
\begin{equation}
\vec{\Omega}(\vec{r}, t) = \nabla \times \vec{A}(\vec{r}, t)
\end{equation}
\end{definition}

\subsection{Die Maxwell-Form der Feldgleichungen}

Die so definierten Felder gehorchen im Vakuum (außerhalb der Quellen) Gleichungen, die formal den Maxwell-Gleichungen der Elektrodynamik entsprechen:

\begin{align}
\nabla \cdot \vec{\Omega} &= 0 \\
\nabla \times \vec{\Omega} &= \frac{4\pi G}{c^2} \vec{j} + \frac{1}{c^2} \frac{\partial}{\partial t} (-\nabla \Phi) \\
\nabla \cdot (-\nabla \Phi) &= -4\pi G\rho \\
\nabla \times (-\nabla \Phi) &= -\frac{\partial \vec{\Omega}}{\partial t}
\end{align}

Diese Differentialgleichungen sind eine alternative Schreibweise der nicht-lokalen Integralgleichungen. Die wahre Natur der Theorie ist die der instantanen, integralen Wechselwirkung.

\subsection{Die Bewegungsgleichung}

Eine Probemasse $m$ am Ort $\vec{r}$ mit der Geschwindigkeit $\vec{v}$ erfährt die Kraft:

\begin{equation}
\vec{F} = m \left( -\nabla \Phi + 2\vec{v} \times \vec{\Omega} - \frac{\partial \vec{A}}{\partial t} \right)
\end{equation}

Dies ist die Lorentz-Kraft der Gravitation.
\begin{itemize}
\item Der Term $-\nabla\Phi$ repräsentiert die radiale Newtonsche Anziehung.
\item Der Term $2\vec{v} \times \vec{\Omega}$ ist die geschwindigkeitsabhängige Coriolis-artige Kraft, die für die tangentialen Trägheitseffekte verantwortlich ist.
\item Der Term $-\partial\vec{A}/\partial t$ beschreibt induzierte Effekte durch instationäre Massenströme.
\end{itemize}

\subsection{Herleitung der Maxwell-Gravitation aus der WG}

Vergleiche den tangentialen Term $\vec{F}_{\text{tan}}$ mit der Lorentz-Kraft $\vec{F} = q(\vec{E} + \vec{v} \times \vec{B})$. Der Term $2(\dot{r} \cdot \vec{v})\vec{v}$ legt nahe, ein effektives Vektorpotential einzuführen:

\begin{equation}
\vec{A}_g = \frac{6GMm}{c^2r} \vec{v}_q
\end{equation}

wobei $\vec{v}_q$ die Geschwindigkeit der Quelle ist. Für eine bewegte Punktmasse ergibt sich damit:

\begin{equation}
\vec{F}_{\text{tan}} = -m \nabla (\vec{v}_q \cdot \vec{A}_g) + \mathcal{O}(v^2/c^2)
\end{equation}

Definiere das gravito-magnetische Feld als Rotation des Vektorpotentials:

\begin{equation}
\vec{B}_g = \nabla \times \vec{A}_g
\end{equation}

Für eine Punktmasse mit konstanter Geschwindigkeit $\vec{v}_q$ ergibt sich:

\begin{equation}
\vec{B}_g = \frac{2G}{c^2} \cdot \frac{\vec{v}_q \times \vec{r}}{r^3}
\end{equation}

Dies ist exakt der gravito-magnetische Dipolterm, der aus der Allgemeinen Relativitätstheorie als Lense-Thirring-Effekt bekannt ist.

Definiere das gravito-elektrische Feld aus dem radialen Anteil der WG:

\begin{equation}
\vec{E}_g = -\frac{GM}{r^2} \left[ 1 - \frac{\dot{r}^2}{c^2} + \beta \frac{r \ddot{r}}{c^2} \right] \hat{r} + \text{Terme aus } \vec{A}_g
\end{equation}

Im statischen Limes ($\dot{r} = 0$, $\ddot{r} = 0$) vereinfacht sich dies zu:

\begin{equation}
\vec{E}_g = -\frac{GM}{r^2} \hat{r}
\end{equation}

\subsection{Die vollständigen Maxwell-Gleichungen der Gravitation}

Mit diesen Definitionen ergeben sich die Maxwell-Gleichungen der Gravitation:

\begin{align}
\nabla \cdot \vec{E}_g &= -4\pi G\rho + \mathcal{O}(v^2/c^2) \\
\nabla \cdot \vec{B}_g &= 0 \\
\nabla \times \vec{E}_g &= -\frac{\partial \vec{B}_g}{\partial t} + \mathcal{O}(v^2/c^2) \\
\nabla \times \vec{B}_g &= \frac{4\pi G}{c^2} \vec{j} + \frac{1}{c^2} \frac{\partial \vec{E}_g}{\partial t} + \mathcal{O}(v^2/c^2)
\end{align}

Im Grenzfall verschwindender Korrekturen ($v/c \rightarrow 0$) gehen sie in die bekannten Maxwell-Gleichungen der Gravitation über.

\subsection{Omega-Wellen: Gravitationswellen in der $\Omega$-Theorie}

Der wichtigste und unterscheidendste Aspekt der $\Omega$-Theorie ist ihre Vorhersage von Gravitationswellen.

\subsubsection{Die fundamentale Natur der Omega-Wellen}

In der $\Omega$-Theorie gibt es keine retardierten Wellen im Sinne der ART. Die Nicht-Lokalität und Instantaneität der Theorie verbietet eine Ausbreitung mit endlicher Geschwindigkeit. Stattdessen existieren instantan korrelierte, kollektive Schwingungen des $\Omega$-Feldes und der Massenverteilung. Wir nennen sie Omega-Wellen.

Ihre Eigenschaften sind:
\begin{itemize}
\item Instantan: Keine Ausbreitungsverzögerung. Eine Störung im $\Omega$-Feld wirkt sofort im gesamten Raum.
\item Nicht-lokal: Die Wellenfunktion ist eine Korrelation des gesamten Systems, keine lokale Störung.
\item Axial: Die Schwingungen sind Wirbel im $\Omega$-Feld, keine transversalen Metrik-\\Deformationen.
\item Kohärent: Sie beschreiben kollektive Bewegungen großer Massensysteme.
\end{itemize}

\subsubsection{Die emergente Retardierung im fraktalen Raum}

Der fundamentale Raum hat die Dimension $D \approx 2,71$ unterhalb von $l_0$. Auf großen Skalen (LIGO-Skalen) erscheint der Raum effektiv glatt. Durch diese Skalenemergenz entsteht eine effektive Retardierung:

\begin{equation}
\vec{\Omega}_{\text{eff}}(\vec{r}, t) = \int d^3 r' K_{\text{eff}}(|\vec{r} - \vec{r}'|) \vec{\Omega}_{\text{fund}}(\vec{r}', t)
\end{equation}

mit einem Kern $K_{\text{eff}}$, der auf großen Skalen wie eine retardierte Greensche Funktion wirkt.

\subsubsection{Die effektive Wellengleichung}

Auf der Makro-Ebene emergiert daraus die bekannte Wellengleichung:

\begin{equation}
\square \vec{\Omega}_{\text{eff}} = \frac{4\pi G}{c^2} \vec{j}_{\text{eff}} + \mathcal{O} \left( \frac{1}{D-3} \right)
\end{equation}

wobei $\square$ der d'Alembert-Operator ist. Die ART ist in dieser Sicht der Grenzfall $D \rightarrow 3$.

\subsubsection{Unterschiede zu ART-Wellen}

\begin{table}[ht]
\centering
\caption{Vergleich zwischen ART-Wellen und fundamentalen Omega-Wellen}
\begin{tabular}{p{4cm}p{4cm}p{4cm}}
\toprule
Eigenschaft & ART-Wellen & Omega-Wellen \\
\midrule
Fundamental & Metrik-Schwingung & $\Omega$-Feld-Schwingung \\
Ausbreitung & Retardiert ($c$) & Instantan (fundamental) \\
Lokalität & Lokal & Nicht-lokal \\
Polarisation & Transversal ($+$, $\times$) & Axial (wirbelartig) \\
Spektrum & Kontinuierlich & Fraktal skaliert \\
Dispersion & Nein (im Vakuum) & Ja (minimal, testbar) \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Vorhersagen}

Die Theorie macht spezifische, von der ART abweichende Vorhersagen:

\begin{enumerate}
\item \textbf{Frequenzabhängige Geschwindigkeit:}
\begin{equation}
v_{\text{phase}}(\omega) = c \left( 1 + \alpha \left( \frac{\omega}{\omega_0} \right)^{D-3} + \dots \right)
\end{equation}
Mit $D \approx 2,71$ ist $D-3 \approx -0,29$, also eine schwache Dispersion bei hohen Frequenzen.

\item \textbf{Fraktales Spektrum:} Die Leistungsverteilung skaliert mit
\begin{equation}
P(\omega) \sim \omega^{-\beta}, \quad \beta = f(D) \approx 0,7
\end{equation}

\item \textbf{Nicht-lokale Korrelationen:} Bei Verschmelzungsereignissen treten instantane\\Korrelationen zwischen weit entfernten Detektoren auf.

\item \textbf{Abweichungen bei hohen Frequenzen:} Oberhalb einer kritischen Frequenz $\omega_c$ (die mit der fraktalen Skala zusammenhängt) weichen die Signale systematisch von ART-Vorhersagen ab.
\end{enumerate}

\subsubsection{Interpretation der LIGO/Virgo-Daten}

Die gemessenen Signale sind mit dieser Theorie vereinbar:
\begin{itemize}
\item Die grobe Form (Frequenzentwicklung, Amplitude) wird durch die ART beschrieben -- das ist der effektive Anteil.
\item Feinstrukturen, Rauschen und Anomalien tragen die fraktale Signatur.
\item Die Ausbreitungsgeschwindigkeit ist innerhalb der Messgenauigkeit $c$ -- die instantane Komponente ist auf LIGO-Skalen nicht direkt auflösbar.
\end{itemize}

\section{Bose-Einstein-Kondensat (BEC)-Objekte}

\subsection{Paarbildung und Bose-Einstein-Kondensation}

Bei hinreichend hohen Dichten bilden Fermionen Cooper-Paare und werden zu effektiven Bosonen. Diese gehen in einen Bose-Einstein-Kondensat (BEC)-Zustand über. In diesem Zustand:
\begin{itemize}
\item Entfällt der Fermi-Druck (der sonst weitere Kompression verhindert)
\item Das Quantenpotential dominiert die repulsive Wechselwirkung
\item Das Objekt schrumpft bis zur fundamentalen Länge $l_0$
\end{itemize}

\subsection{Zustandsgleichung eines BEC-Objekts}

Aus der Gleichgewichtsbedingung zwischen Gravitation und Quantenpotential im fraktalen Raum folgt:

\begin{equation}
P(\rho) = K \cdot \rho^{\gamma}
\end{equation}

mit
\begin{equation}
\gamma = \frac{3}{2}, \quad K = \frac{\hbar^2}{2m_B} \left( \frac{3\pi^2}{g} \right)^{2/3} \cdot \frac{1}{m_B^{2/3}} \cdot f(D)
\end{equation}

wobei $f(D) \approx 1,2$ für $D \approx 2,71$ und $m_B \approx 2m_p$ die Masse eines Cooper-Paares ist.

\subsection{Radius-Masse-Beziehung}

Aus der Gleichgewichtsbedingung $dE/dR = 0$ mit

\begin{equation}
E(R) = -C_G \frac{GM^2}{R^{D-2}} + A \frac{\hbar^2 M}{m_B^2 R^{2D/3}}
\end{equation}

folgt durch Ableitung und Auflösen nach $R$:

\begin{equation}
R^{2-D/3} = \frac{2DA\hbar^2}{3(D-2)C_G Gm_B^2} \cdot \frac{1}{M}
\end{equation}

Mit $D \approx 2,7156$ ergibt sich $2 - D/3 \approx 1,0948$, also:

\begin{equation}
R(M) = K_R \cdot M^{-0.9134}
\end{equation}

Numerisch: $K_R \approx 5,7 \times 10^{13} \text{ m} \cdot \text{kg}^{0,9134}$.

\subsection{Maximale Masse eines BEC-Objekts}

Die minimale Größe ist $R_{\min} = l_0 \approx 1,8 \times 10^{-15}$ m. Daraus folgt:

\begin{equation}
M_{\text{BEC}} = \left(\frac{K_R}{l_0}\right)^{1/0.9134} \approx 3 \times 10^6 M_{\odot}
\end{equation}

Dies ist die maximale Masse eines einzelnen BEC-Objekts. Oberhalb dieser Grenze wäre $R < l_0$, was physikalisch unmöglich ist.

\subsection{Relativistische Korrekturen aus der Weber-Gravitation}

Die Weber-Gravitation führt zu einem modifizierten innersten stabilen Orbit (ISCO). Für eine kreisförmige Bahn ($\dot{r} = 0$, $\ddot{r} = -v^2/r$) liefert die Weber-Gravitation mit $\beta = 0,5$:

\begin{equation}
\frac{mv^2}{r} = \frac{GMm}{r^2} \left(1 + \frac{v^2}{2c^2}\right)
\end{equation}

Auflösen nach $v^2$:

\begin{equation}
v^2 = \frac{GM}{r} \left(1 + \frac{GM}{2c^2r}\right)
\end{equation}

Die Bedingung für den innersten stabilen Orbit ($dv/dr = 0$) ergibt:

\begin{equation}
r_{\text{ISCO}} = \frac{12GM}{c^2}
\end{equation}

Für $M = M_{\text{BEC}}$: $r_{\text{ISCO}} \approx 5,3 \times 10^{10}$ m $\approx 0,35$ AU -- makroskopisch.

\subsection{Beobachtete supermassereiche Objekte}

Beobachtet werden zentrale Objekte in Galaxien mit Massen bis $10^{10}M_{\odot}$. Diese sind keine einzelnen kompakten Objekte, sondern Systeme bestehend aus:
\begin{itemize}
\item Einem zentralen BEC-Objekt mit $M = M_{\text{BEC}} \approx 3 \times 10^6 M_{\odot}$ und $R = l_0$
\item Einer massiven Umgebung (Akkretionsscheibe, Sternhaufen, Gas), die die zusätzliche Masse von $10^9 - 10^{10} M_{\odot}$ enthält
\end{itemize}
Diese Umgebung entsteht durch die kontinuierliche Materieentstehung aus dem Quantenpotential des Kerns und die anschließende Drift nach außen.

\subsection{Strukturelle Analogie zum Atom}

Die beschriebene Struktur -- ein winziger, extrem dichter Kern, umgeben von einer riesigen, dünnen Hülle, mit viel Vakuum dazwischen -- ist analog zum Atom. Im Atom sitzt fast die gesamte Masse im Kern ($\sim 10^{-15}$ m), die Elektronen sind weit draußen ($\sim 10^{-10}$ m). In der BEC-Galaxie ist der Kern ebenfalls winzig ($l_0 \approx 10^{-15}$ m), die Umgebung (Sterne, Gas) erstreckt sich über $10^{21}$ m -- und der Kern trägt nur einen kleinen Teil der Gesamtmasse.

Die fraktale Raumdimension $D \approx 2,71$ verbindet beide Welten -- sie bestimmt sowohl die Feinstrukturkonstante als auch das Massenverhältnis $M_{\text{Gal}}/M_{\text{Kern}} \approx 1000$.

\subsection{Kollisionen von BEC-Objekten}

BEC-Objekte haben keine Sonderstellung. Bei Kollisionen verhalten sie sich wie alle anderen physikalischen Objekte:
\begin{itemize}
\item \textbf{Verschmelzung:} Wenn $M_1 + M_2 \le M_{\text{BEC}}$, verschmelzen sie zu einem neuen BEC-Objekt.
\item \textbf{Abprallen:} Wenn die Relativgeschwindigkeit hoch genug ist, prallen sie elastisch auseinander.
\item \textbf{Zerrüttung:} Bei geringer Relativgeschwindigkeit und $M_1 + M_2 > M_{\text{BEC}}$ zerreißen die Kerne; die Masse wird in die Umgebung überführt.
\item \textbf{Doppelkern-Systeme:} Die Kerne bleiben getrennt und umkreisen sich in einer gemeinsamen Umgebung.
\end{itemize}

\subsection{Gravitationswellen von BEC-Objekten}

Verschmelzungen von BEC-Objekten erzeugen Gravitationswellen -- ähnlich wie Neutronensterne oder Schwarze Löcher in der ART. Unterschiede:
\begin{itemize}
\item BEC-Objekte haben keinen Ereignishorizont, sondern eine Oberfläche
\item Die Wellenform weicht ab, weil die Materie nicht \enquote{verschluckt} wird
\item Nach der Verschmelzung gibt es Nachglühen
\end{itemize}
Die LIGO/Virgo-Daten sind mit beiden Modellen konsistent, da die beobachteten Massen ($\sim 10-100 M_{\odot}$) weit unter $M_{\text{BEC}}$ liegen.

\section{Das Neutrino als Q-Teilchen}

\subsection{Die Rolle des Neutrinos in der WDBT+}

In der Standardphysik ist das Neutrino ein rätselhaftes Teilchen: Es besitzt keine elektrische Ladung, eine verschwindend kleine, aber nicht verschwindende Masse und wechselwirkt nur schwach. In der WDBT+ ergibt sich eine klare, fundamentale Rolle: Das Neutrino ist das reine Q-Teilchen -- die materielle Manifestation des de Broglie-Bohm-Quantenpotentials $Q$.

\begin{definition}[Neutrino]
Das Neutrino ist ein Teilchen, das ausschließlich über das Quantenpotential $Q$ wechselwirkt. Es besitzt keine elektrische Ladung (keine Kopplung an die WED) und keine Ruhemasse im herkömmlichen Sinne. Seine Masse emergiert aus der Energie des Quantenpotentials, das es trägt.
\end{definition}

\subsection{Das Quantenpotential als Ursache der Nichtlokalität}

In der de Broglie-Bohm-Theorie lautet das Quantenpotential:

\begin{equation}
Q = -\frac{\hbar^2}{2m} \frac{\nabla^2 R}{R}, \quad R = \sqrt{\rho}.
\end{equation}

Es ist nichtlokal, wirkt instantan über das gesamte System und ist für die Führung der Teilchen verantwortlich. In der WDBT+ wird $Q$ zum Ausdruck der globalen, nichtlokalen Informationsorganisation.

Das Neutrino ist der physikalische Träger dieses Quantenpotentials. Während geladene Teilchen (Elektronen, Quarks) über die WED wechselwirken und massive Teilchen (Protonen, Neutronen) über die WG, vermittelt das Neutrino die nichtlokale Organisation des Informationsfeldes.

\subsection{Herleitung der Neutrinomasse}

Die fundamentale Längenskala der Theorie ist $l_0 \approx 1,8 \times 10^{-15}$ m. Sie ist die kleinste physikalisch mögliche Distanz. Die Compton-Wellenlänge des Neutrinos ist:

\begin{equation}
\lambda_{\nu} = \frac{\hbar}{m_{\nu}c}.
\end{equation}

Die Compton-Wellenlänge ist identisch mit der fundamentalen Länge:

\begin{equation}
\lambda_{\nu} = l_0 \quad \Rightarrow \quad \frac{\hbar}{m_{\nu}c} = l_0.
\end{equation}

Daraus folgt unmittelbar:

\begin{equation}
\boxed{m_{\nu} = \frac{\hbar}{l_0c}}.
\end{equation}

Einsetzen der Zahlenwerte ($\hbar = 1,0546 \times 10^{-34}$ J·s, $c = 2,9979 \times 10^8$ m/s, $l_0 = 1,8 \times 10^{-15}$ m) liefert:

\begin{equation}
m_\nu \approx \frac{1,0546 \times 10^{-34}}{1,8 \times 10^{-15} \cdot 2,9979 \times 10^8} \text{ kg} \approx 1,95 \times 10^{-37} \text{ kg}.
\end{equation}

Umrechnung in Elektronenvolt ($1\ \text{eV}/c^2 = 1,7827 \times 10^{-36}$ kg):

\begin{equation}
m_\nu \approx \frac{1,95 \times 10^{-37}}{1,7827 \times 10^{-36}} \text{ eV}/c^2 \approx 0,11 \text{ eV}/c^2.
\end{equation}

Die Theorie sagt also voraus:

\begin{equation}
m_\nu \approx 0,1 \text{ eV}/c^2.
\end{equation}

Dies stimmt mit den beobachteten Größenordnungen überein (Elektron-Neutrino-Masse $< 0,5\ \text{eV}/c^2$).

\subsection{Das Neutrino als Vermittler der Nichtlokalität}

Die physikalische Konsequenz ist:
\begin{itemize}
\item Die Nichtlokalität der Quantenmechanik wird durch ein reales Teilchen -- das Neutrino -- vermittelt.
\item Das Quantenpotential $Q$ ist keine abstrakte Funktion mehr, sondern die Energie des Neutrino-Feldes.
\item Die Bewegung eines geladenen Teilchens wird nicht nur durch lokale Kräfte (WED, WG) bestimmt, sondern auch durch den Fluss von Neutrinos, die das Quantenpotential tragen.
\end{itemize}

\subsection{Drei Neutrino-Generationen}

Die drei bekannten Neutrino-Generationen ($\nu_e$, $\nu_\mu$, $\nu_\tau$) entsprechen drei Moden des Q-Feldes -- einer Projektion der fraktalen Raumdimension $D \approx 2,71$ auf drei diskrete Frequenzbänder. Die beobachteten Massenunterschiede zwischen den Generationen resultieren aus einer feinen Aufspaltung durch die fraktale Geometrie.

\subsection{Konsequenzen für die Dunkle Materie}

Da Neutrinos überall vorhanden sind, kaum wechselwirken und eine kleine Masse von $\approx 0,1\ \text{eV}/c^2$ besitzen, sind sie die Dunkle Materie. In der WDBT+ wird die Dunkle Materie nicht als separate Entität benötigt -- die Gesamtheit der Neutrinos im Kosmos erzeugt die beobachteten gravitativen Effekte.

\section{Fraktale Maxwell-Gleichungen}

\subsection{Der fraktale Nabla-Operator $\nabla_D$}

Im fraktalen Raum mit Dimension $D$ müssen Differentialoperatoren die Skalierungseigenschaften des Raumes berücksichtigen. Der fraktale Nabla-Operator $\nabla_D$ wirkt auf eine Funktion $f$ wie folgt:

\begin{equation}
\nabla_D f = \lim_{\epsilon \to 0} \frac{f(x + \epsilon\hat{n}) - f(x)}{\epsilon^{D/3}}
\end{equation}

Oder in Komponentenschreibweise mit einer fraktalen Ableitung:

\begin{equation}
\frac{\partial^D f}{\partial x^D} = \lim_{\Delta x \to 0} \frac{f(x + \Delta x) - f(x)}{(\Delta x)^{D/3}}
\end{equation}

Diese Definition stellt sicher, dass die Einheiten konsistent bleiben und die fraktale Dimension $D$ in die Differentiation eingeht. Für $D = 3$ reduziert sie sich auf die gewöhnliche Ableitung.

Der fraktale Laplace-Operator ist entsprechend:

\begin{equation}
\nabla_D^2 f = \nabla_D \cdot (\nabla_D f)
\end{equation}

\subsection{Die fraktalen Maxwell-Gleichungen der Elektrodynamik}

Mit diesem Operator lauten die Maxwell-Gleichungen im fraktalen Raum:

\subsubsection{Gaußsches Gesetz (elektrisch)}
\begin{equation}
\nabla_D \cdot \mathbf{E} = \frac{\rho}{\epsilon_0} \cdot \left( \frac{r}{r_0} \right)^{D-3}
\end{equation}
Der Skalierungsfaktor $(r/r_0)^{D-3}$ besagt, dass die effektive Quellstärke im fraktalen Raum vom Abstand abhängt. Für $D = 3$ verschwindet dieser Faktor, und wir erhalten das bekannte Gesetz.

\subsubsection{Gaußsches Gesetz (magnetisch)}
\begin{equation}
\nabla_D \cdot \mathbf{B} = 0
\end{equation}
Die Quellenfreiheit des magnetischen Feldes bleibt erhalten -- sie ist eine topologische Eigenschaft.

\subsubsection{Faradaysches Induktionsgesetz}
\begin{equation}
\nabla_D \times \mathbf{E} = -\frac{\partial \mathbf{B}}{\partial t} \cdot \left( \frac{r}{r_0} \right)^{D-3}
\end{equation}

\subsubsection{Ampèresches Gesetz (mit Maxwell-Ergänzung)}
\begin{equation}
\nabla_D \times \mathbf{B} = \mu_0 \mathbf{j} \cdot \left( \frac{r}{r_0} \right)^{D-3} + \mu_0 \epsilon_0 \frac{\partial \mathbf{E}}{\partial t} \cdot \left( \frac{r}{r_0} \right)^{D-3}
\end{equation}

\subsection{Die fraktalen Maxwell-Gleichungen der Gravitation}

Für die Gravitation existiert eine analoge Struktur. Aus der DSTT folgen zwei Felder:
\begin{itemize}
\item Das gravito-elektrische Feld $\vec{E}_g$ (entspricht der radialen Schwerewirkung)
\item Das gravito-magnetische Feld $\vec{B}_g$ (entspricht der tangentialen Trägheitsantwort)
\end{itemize}

Im fraktalen Raum lauten ihre Gleichungen:

\begin{align}
\nabla_D \cdot \vec{E}_g &= -4\pi G \rho_{\text{eff}} \cdot \left( \frac{r}{r_0} \right)^{D-3} \\
\nabla_D \cdot \vec{B}_g &= 0 \\
\nabla_D \times \vec{E}_g &= -\frac{\partial \vec{B}_g}{\partial t} \cdot \left( \frac{r}{r_0} \right)^{D-3} \\
\nabla_D \times \vec{B}_g &= \mu_g \vec{j}_g \cdot \left( \frac{r}{r_0} \right)^{D-3} + \mu_g \epsilon_g \frac{\partial \vec{E}_g}{\partial t} \cdot \left( \frac{r}{r_0} \right)^{D-3}
\end{align}

Dabei sind $\rho_{\text{eff}}$ die effektive Massendichte, $\vec{j}_g$ der Massenstrom, und $\epsilon_g$, $\mu_g$ die entsprechenden Konstanten mit $\epsilon_g\mu_g = 1/c^2$.

\subsection{Die Wellengleichung im fraktalen Raum}

Aus den fraktalen Maxwell-Gleichungen folgt eine modifizierte Wellengleichung. Für das elektrische Feld im Vakuum:

\begin{equation}
\nabla_D^2 \mathbf{E} - \frac{1}{c^2} \frac{\partial^2 \mathbf{E}}{\partial t^2} = 0
\end{equation}

Die Eigenfunktionen sind keine ebenen Wellen $e^{i(kx-\omega t)}$, sondern fraktale Wellenfunktionen. Im Zentralfeld lautet die Lösung:

\begin{equation}
\mathbf{E}(r, \phi, t) = \mathbf{E}_0 \cdot r^{D-2} \cdot e^{i(k\phi - \omega t)}
\end{equation}

Das sind Spiralwellen -- genau wie die Bahnformen der DSTT. Die Amplitude skaliert mit $r^{D-2} \approx r^{0,71}$ (für $D \approx 2,71$), und die Phase ist proportional zum Winkel $\phi$.

Für die Gravitation ergibt sich analog:

\begin{equation}
\vec{E}_g(r, \phi, t) = \vec{E}_{g0} \cdot r^{D-2} \cdot e^{i(k\phi - \omega t)}
\end{equation}

\subsection{Dispersionsrelation}
\label{sec:dispersion}

Für ebene Wellen (Näherung für große Entfernungen) ergibt sich eine Dispersionsrelation:

\begin{equation}
\omega^2 = c^2 k^2 \left( 1 + \alpha \left( \frac{k}{k_0} \right)^{D-3} + \ldots \right)
\end{equation}

Mit $D \approx 2,71$ ist $D-3 \approx -0,29$, also:

\begin{equation}
\omega^2 = c^2 k^2 \left( 1 + \alpha \left( \frac{k}{k_0} \right)^{-0.29} + \ldots \right)
\end{equation}

Das bedeutet: Licht und Gravitationswellen sind im fraktalen Raum minimal dispersiv. Verschiedene Frequenzen breiten sich mit leicht unterschiedlicher Geschwindigkeit aus -- ein Effekt, der bei extrem hohen Frequenzen oder über kosmische Distanzen messbar ist.

\subsection{Konsequenzen}

\subsubsection{Erklärung der Feinstrukturkonstante}
Die Feinstrukturkonstante $\alpha \approx 1/137$ ergibt sich aus der Dimension $D$:

\begin{equation}
\alpha(D) = \alpha_0 \cdot f(D) \quad \text{mit} \quad f(D) = \left( \frac{D-2}{D} \right)^D
\end{equation}

Für $D \approx 2,71$ ergibt sich $\alpha \approx 1/137$.

\subsubsection{Keine Unendlichkeiten -- keine Renormierung}
In der fraktalen Elektrodynamik sind viele Integrale, die in der QED divergieren, automatisch endlich. Der Grund: Die fraktale Dimension $D < 3$ wirkt als natürlicher Cutoff. Die Renormierung wird überflüssig.

\subsubsection{Einheit von Elektrodynamik und Gravitation}
Die fraktalen Maxwell-Gleichungen für Elektrodynamik und Gravitation haben dieselbe mathematische Struktur. Sie unterscheiden sich nur in den Konstanten und der Interpretation der Felder. Dies ist die Einheit der Physik -- durch den gemeinsamen fraktalen Raum.

\section{Kosmologie: Stationäres Universum ohne Urknall}

Die WDBT+ führt zu einem stationären, nicht-expandierenden Universum.

\subsection{Die kosmische Hintergrundstrahlung (CMB)}
Die kosmische Hintergrundstrahlung ist kein Nachglühen des Urknalls, sondern eine emergente Eigenschaft der fraktalen Entropie-Zirkulation zwischen Vakuum, Materie und $\Omega$-Feld. Ihr Spektrum ergibt sich aus der fraktalen Geometrie ohne freie Parameter. Die Planck-Verteilung folgt aus dem Gleichgewicht zwischen Emissions- und Absorptionsprozessen im stationären Universum, wobei die fraktale Dimension $D \approx 2,71$ die spektrale Energiedichte modifiziert. Die gemessene Temperatur $T \approx 2,725$ K ist eine Konsequenz der fundamentalen Länge $l_0$ und der Dichte des Neutrino-Hintergrunds.

\subsection{Hubble-Beziehung ohne Expansion}
Die lineare Hubble-Beziehung ist keine Dopplerverschiebung durch Expansion, sondern ein kumulativer Effekt der DSTT-Drift über kosmische Distanzen. Die Rotverschiebung $z = H_0 \cdot d/c$ folgt aus der Mittelung über viele Galaxien. Für eine Galaxie in Entfernung $d$ addieren sich die Driftbeiträge aller dazwischenliegenden Massen:
\begin{equation}
z = \frac{1}{c} \int_0^d \frac{v_{\text{drift}}(r)}{\lambda(r)} dr \approx \frac{H_0}{c} d,
\end{equation}
wobei $H_0$ aus der mittleren Massendichte des Universums folgt. Die Theorie sagt Abweichungen vom linearen Hubble-Gesetz auf sehr großen Skalen voraus.

\subsection{Urknall-Nukleosynthese}
Die Häufigkeiten von Wasserstoff, Helium und Lithium sind keine Relikte eines Urknalls, sondern eine Folge der kontinuierlichen Materieentstehung aus dem Quantenpotential in den ersten Sekunden des stationären Universums. Die spezifischen Verhältnisse ergeben sich aus der fraktalen Dimension $D$. Die Entstehungsraten für verschiedene Kerne folgen aus der fraktalen Skalierung:
\begin{equation}
\dot{n}_A \propto n_{\text{cre}} \cdot P_A(D),
\end{equation}
wobei $P_A(D)$ die Wahrscheinlichkeit für die Bildung eines Kerns mit Massenzahl $A$ im fraktalen Raum ist. Für $D \approx 2,71$ reproduziert die Theorie die beobachteten Häufigkeiten ohne freie Parameter.

\subsection{Rotverschiebung}

Die beobachtete Rotverschiebung ist keine Doppler-Folge der Expansion, sondern eine kumulative gravitative Wirkung auf Photonen während ihrer Reise durch das $\Omega$-Feld. Da $\beta$ für Photonen von der Energie abhängt, zeigen verschiedene Frequenzen leicht unterschiedliche Rotverschiebungen. Dieser Effekt ist klein, aber mit Pulsar-Laufzeiten oder hochpräzisen Spektren messbar.

\subsection{Galaxienrotation}

Flache Rotationskurven werden ohne Dunkle Materie durch das $\Omega$-Feld und die anisotrope Trägheit der DSTT erklärt. Die radiale Dichteverteilung der Galaxie ergibt sich aus der kontinuierlichen Materieentstehung und der DSTT-Drift. Aus der Kontinuitätsgleichung und der Driftgeschwindigkeit $v_{\text{drift}}(r) = B \sqrt{GM_{\text{Kern}}/r^3}$ folgt:

\begin{equation}
\rho_{\text{Gal}}(r) \propto r^{-0.21}
\end{equation}

Die Rotationskurve folgt daraus:

\begin{equation}
v(r) = \sqrt{\frac{GM(r)}{r}} \propto \sqrt{r^{2.79}-1} = r^{0.895}
\end{equation}

Dies ist eine fast flache Rotationskurve, konsistent mit Beobachtungen.

\subsection{CMB-Dipol}

Der beobachtete Dipol im Mikrowellenhintergrund ist eine Superposition aus der lokalen Bewegung des Sonnensystems und einem intrinsischen Anteil, der von einem kosmologischen $\vec{\Omega}_{\text{kosmisch}}$-Feld herrührt.

\subsection{Zeitpfeil}

Die Bedingung $\dot{\beta} > 0$ liefert einen intrinsischen kosmologischen Zeitpfeil, der nicht aus der Thermodynamik abgeleitet werden muss.

\subsection{Materieentstehung und Galaxienwachstum}

Das Quantenpotential erzeugt neue Materie im Vakuum:

\begin{equation}
\dot{\rho}_{\text{cre}}(r) \propto Q(r) \propto \frac{1}{r^2}
\end{equation}

Im fraktalen Raum mit $D \approx 2,71$:

\begin{equation}
\frac{dM_{\text{cre}}}{d\ln r} \propto r^{3-D} = r^{0.29}
\end{equation}

Die DSTT-Driftgeschwindigkeit:

\begin{equation}
v_{\text{drift}}(r) = B \sqrt{\frac{GM_{\text{Kern}}}{r^3}}
\end{equation}

Die Einfangwahrscheinlichkeit:

\begin{equation}
\alpha_{\text{in}} = \left( \frac{r_c}{r_{\text{max}}} \right)^{3-D} \approx 0.001
\end{equation}

Aus der Drift folgt für die kumulierte Masse:

\begin{equation}
M_{\text{Gal}}(t) \propto t^{1.116}
\end{equation}

Das Massenverhältnis:

\begin{equation}
\frac{M_{\text{Gal}}}{M_{\text{Kern}}} = \frac{1 - \alpha_{\text{in}}}{\alpha_{\text{in}}} \approx 1000
\end{equation}

Mit $M_{\text{Kern}} = M_{\text{BEC}} \approx 3 \times 10^6 M_{\odot}$ ergibt sich $M_{\text{Gal}} \approx 3 \times 10^9 M_{\odot}$ -- typisch für große Galaxien.

\subsection{Die Sonne als Mikrokosmos}

Das Modell ist skalierbar. Für $M = M_{\odot}$: $r_{\text{max}} \sim 1,5 \times 10^{13}$ m, $r_c \sim 1,4 \times 10^{-11}$ m, $\alpha_{\text{in}} \sim 1,2 \times 10^{-7}$.

Interpretation des Sonnenwinds:
\begin{itemize}
\item Im Standardmodell wird der Sonnenwind als Materieausstoß aus der Sonnenkorona gesehen.
\item In der WDBT+ hingegen entsteht Materie in der Nähe der Sonne (bei $r \sim 10^{-11}$ m)
\item Die DSTT-Drift treibt sie nach außen
\item Nur ein winziger Bruchteil ($\sim 10^{-7}$) wird von der Sonne eingefangen
\item Die Sonne verliert daher keine signifikante Masse
\end{itemize}

\subsection{Beobachtbare Signaturen: Der EHT-Schatten}

Ein BEC-Objekt mit $M = M_{\text{BEC}}$ hat $R = l_0 \approx 10^{-15}$ m -- viel zu klein, um direkt abgebildet zu werden. Was das Event Horizon Telescope (EHT) zeigt, ist der Einflussbereich: die Region, in der Licht stark abgelenkt wird.

Der Photonenorbit liegt bei $r_{\text{ph}} \approx 3GM/c^2$. Für $M = M_{\text{BEC}}$:

\begin{equation}
r_{\text{ph}} \approx 1,8 \times 10^{10} \text{ m} \approx 0,12 \text{ AU}
\end{equation}

Der Schatten ist also makroskopisch.

Testbare Unterschiede zur ART:
\begin{itemize}
\item Feinstruktur des Schattens
\item Polarisationsmuster
\item Zeitliche Variabilität (Flares)
\end{itemize}

\subsection{Fraktale Skalengesetze in der Kosmologie}

Die fundamentale fraktale Dimension $D \approx 2,71$ ist auf makroskopischen Skalen nicht direkt messbar, da der Raum oberhalb von $l_0$ effektiv glatt ist. Sie erzeugt jedoch \textbf{emergente Skalengesetze} mit fraktalen Exponenten, die auf allen Skalen beobachtbar sind -- einschließlich kosmischer.

\subsubsection{Der Unterschied: Raumdimension vs. Skalenexponent}

\begin{table}[ht]
\centering
\caption{Raumdimension vs. emergenter Skalenexponent}
\begin{tabular}{p{3cm}p{5cm}p{4cm}}
\toprule
& Fundamentale Raumdimension $D$ & Emergenter Skalenexponent \\
\midrule
Gültigkeit & Nur unterhalb $l_0 \approx 10^{-15}$ m & Auf allen Skalen \\
Messbarkeit & Nicht direkt (unterhalb jeder experimentellen Grenze) & Direkt beobachtbar \\
Beispiel & $D \approx 2,71$ & $\rho(r) \propto r^{-0,21}$ \\
Rolle & Fundamentale Geometrie & Dynamische Konsequenz \\
\bottomrule
\end{tabular}
\end{table}

\subsubsection{Die zentralen Skalengesetze der WDBT+}

Aus der Theorie folgen spezifische fraktale Exponenten:

\begin{align}
\text{Galaxien-Dichteprofil:} \quad & \rho_{\text{Gal}}(r) \propto r^{-0,21} \\
\text{Rotationskurve:} \quad & v(r) \propto r^{0,895} \\
\text{CMB-Korrelationen:} \quad & C(\theta) \propto \theta^{-0,7} \\
\text{Materieentstehung:} \quad & \frac{dM_{\text{cre}}}{d\ln r} \propto r^{0,29} \\
\text{Galaxienwachstum:} \quad & M_{\text{Gal}}(t) \propto t^{1,116}
\end{align}

\subsubsection{Warum das $\Lambda$CDM diese Exponenten nicht erklären kann}

Im Standardmodell mit glattem Raum ($D=3$) sind alle Skalenexponenten ganzzahlig oder halbzahlig:
\begin{itemize}
\item $\rho(r) \propto r^{-2}$ (isothermes Profil)
\item $v(r) \propto r^{-0,5}$ außerhalb der sichtbaren Masse (Kepler-Fall)
\item $C(\theta)$ ohne bevorzugte Richtung
\end{itemize}

Die WDBT+ sagt gebrochene Exponenten voraus -- eine klare Signatur der fraktalen Mikrostruktur.

\subsubsection{Beobachtbare Konsequenzen}

Diese Skalengesetze sind testbar:
\begin{enumerate}
\item \textbf{Galaxienumfragen:} Das Dichteprofil $\rho \propto r^{-0,21}$ ist flacher als das NFW-Profil ($\rho \propto r^{-1}$ im inneren Bereich).
\item \textbf{Rotationskurven:} Bei sehr großen Radien ($r > 100$ kpc) sollte $v(r)$ nicht abfallen, sondern langsam ansteigen.
\item \textbf{CMB:} Die \enquote{Achse des Bösen} ist keine statistische Fluktuation, sondern eine feste, berechenbare Ausrichtung aus der Dodekaeder-Symmetrie.
\end{enumerate}

\subsubsection{Analogie}

Die Situation ist analog zur turbulenten Strömung: Die Navier-Stokes-Gleichungen sind lokal gültig, aber die großskalige Turbulenz zeigt fraktale Skalengesetze (Kolmogorov-Spektrum $E(k) \propto k^{-5/3}$). Hier ist der Raum lokal glatt, aber die Dynamik erzeugt fraktale Strukturen auf allen Skalen.

\section{Vergleich mit dem Standardmodell}

\begin{table}[ht]
\centering
\caption{Gegenüberstellung der Standardkosmologie und der WDBT+}
\begin{tabular}{p{4cm}p{4.5cm}p{4.5cm}}
\toprule
Phänomen & Standardmodell & WDBT+ \\
\midrule
Zentrale Objekte & Senken (verschlingen Materie) & Quellen (erzeugen Materie) \\
Materiefluss & Einwärts (Akkretion) & Auswärts (Drift) \\
Galaxien & Kollaps + Dunkle Materie & Wachstum von innen nach außen \\
Universum & Expandierend, Urknall & Stationär, ewig \\
Schwarze Löcher & Singularität + Horizont & BEC-Objekt, kein Horizont \\
Maximale Masse (Kern) & Keine natürliche Grenze & $M_{\text{BEC}} \approx 3\times 10^6 M_{\odot}$ \\
Super-massereich & Einzelnes Schwarzes Loch & BEC-Kern + Umgebung \\
Kollisionen & Immer Verschmelzung & Wie normale Objekte \\
Sonnenwind & Ausstoß aus der Korona & Neu entstehende Materie \\
Dunkle Materie & Benötigt & Nicht benötigt \\
Dunkle Energie & Benötigt & Nicht benötigt \\
CMB & Urknall-Relikt & Entropie-Zirkulation \\
Hubble-Gesetz & Expansion & DSTT-Drift \\
Nukleosynthese & Urknall & Kontinuierliche Entstehung \\
Skalenexponenten & Ganzzahlig/halbzahlig & Gebrochen (fraktal) \\
\bottomrule
\end{tabular}
\end{table}

\section{Zusammenhang zwischen WG, DSTT und Maxwell-Gravitation}

\subsection{Die Identitätsbeziehung}

Definiere die folgende Identität, die die vektorielle Aufteilung der WG-Kraft mit der energetischen Aufteilung der DSTT verknüpft:

\begin{equation}
\frac{|\vec{F}_{\text{WG}}^{\text{tan}}|}{|\vec{F}_{\text{WG}}^{\text{rad}}| + |\vec{F}_{\text{WG}}^{\text{tan}}|} \equiv \beta_{\text{DSTT}}(t)
\end{equation}

Dabei sind:

\begin{align}
\vec{F}_{\text{WG}}^{\text{rad}} &= -\frac{GMm}{r^2} \left[ 1 - \frac{\dot{r}^2}{c^2} + \beta_{\text{WG}} \frac{r\ddot{r}}{c^2} \right] \hat{r} \\
\vec{F}_{\text{WG}}^{\text{tan}} &= -\frac{GMm}{r^2} \cdot \frac{2(\dot{r} \cdot \vec{v})}{c^2} \vec{v}
\end{align}

Einsetzen ergibt:

\begin{equation}
\frac{\frac{2(\dot{r} \cdot \vec{v})^2}{c^2}}{1 - \frac{\dot{r}^2}{c^2} + \beta_{\text{WG}} \frac{r\ddot{r}}{c^2} + \frac{2(\dot{r} \cdot \vec{v})^2}{c^2}} = \beta_{\text{DSTT}}(t)
\end{equation}

\subsection{Die Kopplungsgleichung}

Aus den Bewegungsgleichungen der WG und der Definition von $\beta_{\text{DSTT}}(t)$ folgt:

\begin{equation}
\dot{\beta}_{\text{DSTT}} = \frac{2\dot{r}}{r} \cdot \frac{GM}{c^2r} \cdot \frac{1 - \beta_{\text{WG}}}{\left(1 + \frac{\dot{r}^2}{r^2\dot{\phi}^2}\right)^2} + \mathcal{O}\left(\frac{1}{c^4}\right)
\end{equation}

Dies ist das zentrale Ergebnis:
\begin{itemize}
\item Für $\beta_{\text{WG}} < 1$ ist $\dot{\beta}_{\text{DSTT}} > 0$ (bis auf Punkte mit $\dot{r} = 0$)
\item Für $\beta_{\text{WG}} = 1$ ist $\dot{\beta}_{\text{DSTT}} = 0$ (im Rahmen der Näherung)
\item Für $\beta_{\text{WG}} > 1$ wäre $\dot{\beta}_{\text{DSTT}} < 0$
\end{itemize}
Mit $\beta_{\text{WG}} = 0,5$ für Massen folgt zwingend $\dot{\beta}_{\text{DSTT}} > 0$. Die WG enthält also implizit die Monotonie der DSTT.

\subsection{Übertragung auf die Elektrodynamik}

Die Weber-Elektrodynamik (WED) hat eine analoge Struktur mit $\beta_{\text{WED}} = 2$. Die analoge Herleitung ergibt:

\begin{equation}
\dot{\beta}_{\text{DSTT}}^{\text{(EM)}} = \frac{2\dot{r}}{r} \cdot \frac{kq_1q_2}{c^2r\mu} \cdot \frac{1 - \beta_{\text{WED}}/2}{\left(1 + \frac{\dot{r}^2}{r^2\dot{\phi}^2}\right)^2}
\end{equation}

Mit $\beta_{\text{WED}} = 2$ ist $1 - \beta_{\text{WED}}/2 = 0$, also:

\begin{equation}
\dot{\beta}_{\text{DSTT}}^{\text{(EM)}} = 0
\end{equation}

Dies erklärt den fundamentalen Unterschied zwischen Gravitation und Elektrodynamik:
\begin{itemize}
\item Gravitation ($\beta_{\text{WG}} = 0,5$): $\dot{\beta}_{\text{DSTT}} > 0$ $\rightarrow$ Spiralbahnen, Auswärtsdrift, Irreversibilität
\item Elektrodynamik ($\beta_{\text{WED}} = 2$): $\dot{\beta}_{\text{DSTT}} = 0$ $\rightarrow$ stabile Bahnen (Ellipsen), Reversibilität
\end{itemize}

\section{Die Theorie der fraktalen Entropie}

\subsection{Axiome}

\begin{axiom}[Fraktale Raumstruktur]
Der fundamentale Raum ist eine fraktale Menge $\mathcal{M}$ mit Hausdorff-Dimension $D$:
\begin{equation}
D = \frac{\ln 20}{\ln(2 + \phi)} \approx 2.71, \quad \phi = \frac{1 + \sqrt{5}}{2}
\end{equation}
Die fundamentale Längenskala ist $l_0 \approx 1,8 \times 10^{-15}$ m.
\end{axiom}

\begin{axiom}[Fraktale Entropie]
Für eine Wahrscheinlichkeitsdichte $\rho : \mathcal{M} \rightarrow \mathbb{R}^+$ ist die fraktale Entropie definiert als
\begin{equation}
S_D[\rho] = -k_B \int_{\mathcal{M}} d^D r \, \rho(\vec{r}) \ln \rho(\vec{r}) \cdot \left( \frac{|\vec{r}|}{l_0} \right)^{D-3}
\end{equation}
Für $D = 3$ ergibt sich die Boltzmann-Entropie. Für $D < 3$ ist $S_D$ subextensiv.
\end{axiom}

\begin{axiom}[Globaler Entropieerhaltungssatz]
Die Gesamt-fraktale Entropie des Universums ist konstant:
\begin{equation}
\frac{dS_D^{\text{ges}}}{dt} = 0
\end{equation}
\end{axiom}

\begin{axiom}[Lokale Entropieproduktion]
In jeder einzelnen Phase gilt:
\begin{equation}
\frac{dS_D^{(i)}}{dt} \geq 0
\end{equation}
\end{axiom}

\begin{axiom}[Gravitation als Quelle der Irreversibilität]
Die Weber-Gravitation mit $\beta_{\text{WG}} = 0,5$ führt zu:
\begin{equation}
\dot{\beta}_{\text{DSTT}} > 0 \quad \text{für alle } t
\end{equation}
\end{axiom}

\begin{axiom}[Elektrodynamik als Stabilisator]
Die Weber-Elektrodynamik mit $\beta_{\text{WED}} = 2$ führt zu:
\begin{equation}
\dot{\beta}_{\text{DSTT}} = 0 \quad \text{für alle } t
\end{equation}
\end{axiom}

\begin{axiom}[Beschränktheit aller physikalischen Größen]
Für jede physikalische Größe $X$ gilt:
\begin{equation}
\limsup_{t \to \infty} |X(t)| < \infty
\end{equation}
\end{axiom}

\subsection{Die drei Phasen}

Das Universum besteht aus drei Phasen, zwischen denen die fraktale Entropie zirkuliert:

\begin{itemize}
\item \textbf{Vakuum:} $S_D^{\text{vac}}$ -- die fraktale Struktur des Raums selbst
\item \textbf{Materie:} $S_D^{\text{kin}}$ -- Bewegung der Teilchen
\item \textbf{$\Omega$-Feld:} $S_D^{\Omega}$ -- das Gravitationsfeld als Rotationsfeld
\end{itemize}

Der Erhaltungssatz lautet:
\begin{equation}
S_D^{\text{vac}}(t) + S_D^{\text{kin,grav}}(t) + S_D^{\text{kin,em}}(t) + S_D^{\Omega}(t) = S_D^{\text{ges}} = \text{const}
\end{equation}

\subsection{Entropieflüsse}

Aus den Bewegungsgleichungen der WDBT+ folgt:

\begin{align}
\frac{dS_D^{\text{vac}}}{dt} &= -\sigma_{\text{cre}} + \sigma_{\text{back}} \\
\frac{dS_D^{\text{kin,grav}}}{dt} &= +\sigma_{\text{cre}} - \sigma_{\text{drift}} \\
\frac{dS_D^{\text{kin,em}}}{dt} &= 0 \\
\frac{dS_D^{\Omega}}{dt} &= +\sigma_{\text{drift}} - \sigma_{\text{back}}
\end{align}

mit $\sigma_{\text{cre}} > 0$, $\sigma_{\text{drift}} > 0$, $\sigma_{\text{back}} > 0$.

\begin{satz}[Stationärer Zustand]
Im stationären Zustand gilt $\sigma_{\text{cre}} = \sigma_{\text{drift}} = \sigma_{\text{back}} \equiv \sigma$, und die Entropien jeder Phase sind konstant.
\end{satz}

\subsection{Kein Wärmetod}

\begin{theorem}[Hauptsatz der fraktalen Thermodynamik]
Unter den Axiomen gilt:
\begin{enumerate}
\item Lokale Entropieproduktion: $\frac{dS_D^{(i)}}{dt} \geq 0$ für jede Phase
\item Globale Entropieerhaltung: $\frac{dS_D^{\text{ges}}}{dt} = 0$
\item Permanente Dynamik: $\sigma_{\text{cre}}, \sigma_{\text{drift}}, \sigma_{\text{back}} > 0$ für alle $t$
\item Kein Entropiemaximum: Es existiert kein endlicher Zustand maximaler Gesamtentropie
\item Kein Wärmetod: Das System erreicht nie einen Gleichgewichtszustand mit verschwindenden Gradienten
\end{enumerate}
\end{theorem}

\section{Die fraktalen Q-Feldgleichungen}

\subsection{Die drei Säulen auf Feldebene}

Die WDBT+ basiert auf der Idee, dass die Physik auf fundamentaler Ebene teilchenbasiert und feldlos ist. Jedes Teilchen wechselwirkt direkt mit jedem anderen über drei Beiträge:

\begin{equation}
\vec{F} = \vec{F}_{\text{WED}} + \vec{F}_{\text{WG}} + \vec{F}_Q
\end{equation}

Im Kontinuumslimes emergieren Feldtheorien in der Form fraktaler Maxwell-\\Gleichungen:
\begin{itemize}
\item Aus $\vec{F}_{\text{WED}}$: fraktale Maxwell-Gleichungen der Elektrodynamik ($\vec{E}$, $\vec{B}$)
\item Aus $\vec{F}_{\text{WG}}$: fraktale Maxwell-Gleichungen der Gravitation ($\vec{E}_g$, $\vec{B}_g$)
\item Aus $\vec{F}_Q$: fraktale Q-Feldgleichungen ($\vec{Q}$, $\vec{N}$)
\end{itemize}

\subsection{Die Q-Feldgleichungen}

Wir definieren:
\begin{itemize}
\item $\vec{Q}(\vec{r}, t)$: das Q-Feld (analog zum elektrischen Feld)
\item $\vec{N}(\vec{r}, t)$: das Neutrino-Wirbelfeld (analog zum magnetischen Feld)
\end{itemize}

Die Gleichungen lauten:

\begin{align}
\nabla_D \cdot \vec{Q} &= \frac{\rho_Q}{\epsilon_Q} \left( \frac{r}{r_0} \right)^{D-3}, \quad \rho_Q = m_\nu n_\nu \\
\nabla_D \cdot \vec{N} &= 0 \\
\nabla_D \times \vec{Q} &= -\frac{\partial \vec{N}}{\partial t} \left( \frac{r}{r_0} \right)^{D-3} \\
\nabla_D \times \vec{N} &= \mu_Q \vec{j}_Q \left( \frac{r}{r_0} \right)^{D-3} + \mu_Q \epsilon_Q \frac{\partial \vec{Q}}{\partial t} \left( \frac{r}{r_0} \right)^{D-3}
\end{align}

mit $\vec{j}_Q = \rho_Q \vec{v}_\nu$ und $\epsilon_Q \mu_Q = 1/c^2$.

\subsection{Die vollständige Bewegungsgleichung}

Ein Testteilchen spürt die Überlagerung aller drei Wechselwirkungen:

\begin{equation}
\vec{F} = q(\vec{E} + \vec{v} \times \vec{B}) + m(\vec{E}_g + 2\vec{v} \times \vec{B}_g) + \kappa(\vec{Q} + \vec{v} \times \vec{N})
\end{equation}

Für ein Neutrino ($q=0$, $m \approx m_\nu$) gilt die Selbstkonsistenzbedingung:

\begin{equation}
m_\nu \ddot{\vec{x}}_\nu = \kappa(\vec{Q} + \vec{v}_\nu \times \vec{N})
\end{equation}

\section{Testbare Vorhersagen}

Die Theorie macht spezifische, von der ART abweichende Vorhersagen:

\begin{enumerate}
\item \textbf{Konstanz der Sonnenmasse:} Präzise Messungen zeigen, dass die Sonne keinen messbaren Masseverlust durch den Sonnenwind erfährt.
\item \textbf{Fraktale Skalierung im Sonnenwind:} Fluktuationen des Sonnenwinds zeigen einen Exponenten $0,29$.
\item \textbf{Frequenzabhängige Lichtablenkung:} $\Delta\phi \propto \lambda^2$ (messbar bei Radiogalaxien).
\item \textbf{Modifizierter Lamb-Shift:} Zusatzterm durch Vakuum-Quantenpotential.
\item \textbf{Frequenzabhängige Shapiro-Laufzeit:} Testbar mit Pulsarsignalen.
\item \textbf{Fraktale Signaturen im Plasma:} Stromdichten skalieren mit $j(r) \propto r^{-0.29}$.
\item \textbf{Maximale Kernmasse:} Zentralobjekte in Galaxien haben eine Obergrenze bei $\sim 3 \times 10^6 M_{\odot}$.
\item \textbf{Doppelkern-Systeme:} Bei Galaxienverschmelzungen sind Doppelkerne häufiger als in der ART vorhergesagt.
\item \textbf{Dichteprofil von Galaxien:} $\rho(r) \propto r^{-0.21}$.
\item \textbf{Rotationskurven:} Bei $r > 100$ kpc fällt die Kurve nicht ab, sondern steigt langsam an.
\item \textbf{EHT-Schatten:} $r_{\text{ph}} \approx 3GM/c^2$, aber Feinstruktur und Variabilität unterscheiden sich.
\item \textbf{Gravitationswellen:} Zusätzliches Nachglühen bei Verschmelzungen; Dispersion.
\item \textbf{Spiralbahnen statt Ellipsen:} Langsame Spiralbewegung (radiale Drift).
\item \textbf{Hubble-Gesetz:} Abweichungen vom linearen Verlauf auf großen Skalen.
\item \textbf{Neutrinomasse:} $m_\nu = \hbar/(l_0c) \approx 0,1\ \text{eV}/c^2$.
\item \textbf{Drei Neutrino-Generationen:} Als Moden des Quantenpotentials.
\item \textbf{Neutrino-Oszillationen:} $L_{\text{osc}} \propto E^{0.775}$.
\item \textbf{Dunkle Materie:} Neutrino-See, keine separaten Teilchen.
\item \textbf{Nichtlokalität:} In Bell-Tests Korrelationen schneller als Licht.
\item \textbf{CMB-Spektrum:} Planck-Verteilung ohne Urknall, aus fraktaler Entropie.
\item \textbf{Skalenexponenten:} Gebrochene Exponenten in Dichteprofilen, Rotationskurven und CMB-Korrelationen.
\end{enumerate}

\section{Diskussion}

\subsection{Warum die ART unvollständig ist}

Die ART basiert auf drei Säulen:
\begin{enumerate}
\item Das Äquivalenzprinzip (Ursache = Wirkung)
\item Die gekrümmte Raumzeit (Geometrie als Ursache)
\item Die Feldgleichungen (Materie krümmt Raumzeit)
\end{enumerate}

Die DSTT zeigt: Säule 1 ist falsch. Ursache und Wirkung sind nicht identisch. Die Trägheitsreaktion ist anisotrop. Das Äquivalenzprinzip ist eine Idealisierung.

Die ART ist daher prinzipiell unvollständig -- sie ist ein emergenter Grenzfall der DSTT.

\subsection{Die ART als emergenter Grenzfall}

Die ART funktioniert, wenn:
\begin{itemize}
\item $\beta \approx$ konstant (keine merkliche Drift)
\item $\dot{r} \approx 0$ (quasi-geschlossene Bahnen)
\item Die anisotrope Trägheit vernachlässigt werden kann
\end{itemize}

In diesem Grenzfall kann die Spiralbahn durch eine präzedierende Ellipse angenähert werden. Die ART beschreibt diese Präzession, übersieht aber die zugrundeliegende Drift.

\subsection{Die entscheidende Frage}

Warum wurde das nicht längst erkannt? Weil die Drift extrem langsam ist. Für die Erde um die Sonne beträgt sie:
\begin{equation}
\frac{\dot{r}}{r} \approx 10^{-11} \text{ pro Jahr}
\end{equation}
Das entspricht wenigen Millimetern pro Jahr. Kepler und Newton sahen daher geschlossene Ellipsen.

Auf kosmischen Skalen summiert sich diese Drift:
\begin{itemize}
\item Jupiter ist nach außen gewandert -- mehr als Gezeitenmodelle erklären.
\item Der Mond driftet nach außen -- schneller als Gezeitenmodelle vorhersagen.
\item Galaxien zeigen flache Rotationskurven -- durch andere Trägheitsdynamik.
\item Die Rotverschiebung sieht wie Expansion aus -- ist aber Summe vieler Driften.
\end{itemize}

\section{Antwort auf häufige Einwände}

Die WDBT+ ist eine junge, umfassende Theorie. Sie erhebt nicht den Anspruch, bereits in allen mathematischen Details ausgearbeitet zu sein. Sie erhebt jedoch den Anspruch, auf jede fundamentale Einwendung eine klare Antwort zu geben. Die folgende Zusammenstellung adressiert die vier häufigsten Kritikpunkte.

\subsection{Einwand 1: Die fraktalen Differentialoperatoren sind nicht rigoros definiert}

Dieser Einwand ist berechtigt, aber er verfehlt die Struktur der Theorie.

Die fundamentalen Gleichungen der WDBT+ sind \textbf{Integralgleichungen}, keine Differentialgleichungen. Die Weber-Kraft ist als direktes Teilchengesetz formuliert. Die fraktalen Maxwell-Gleichungen in Differentialform sind eine \textbf{emergente Näherung} für den Fall $D \approx 3$. Für $D = 3$ reduzieren sie sich auf die bekannten Gleichungen. Für $D \approx 2,71$ sind sie als asymptotische Reihe in $(D-3)$ zu verstehen:

\begin{equation}
\nabla_D \cdot \vec{E} = \frac{\rho}{\epsilon_0} \left( \frac{r}{r_0} \right)^{D-3} = \frac{\rho}{\epsilon_0} \left(1 + (D-3)\ln\frac{r}{r_0} + \cdots \right)
\end{equation}

Die Integralform der Theorie ist wohldefiniert. Die Differentialform ist eine praktische Näherung. Eine rigorose Theorie der fraktalen Differentialgeometrie ist ein eigenes Forschungsprogramm, das über den Rahmen dieser Arbeit hinausgeht. Die WDBT+ liefert die physikalischen Gleichungen; die Mathematik muss folgen.

\subsection{Einwand 2: Die Vorhersagen müssen gegen Daten getestet werden}

Dieser Einwand ist nicht nur berechtigt, sondern \textbf{zentral}. Die WDBT+ macht konkrete, quantitative Vorhersagen. Die folgende Tabelle fasst die wichtigsten testbaren Vorhersagen zusammen:

\begin{table}[ht]
\centering
\caption{Wichtigste testbare Vorhersagen der WDBT+}
\begin{tabular}{p{5cm}p{3cm}p{5cm}}
\toprule
Vorhersage & Wert & Testmöglichkeit \\
\midrule
Galaxien-Dichteprofil & $\rho(r) \propto r^{-0,21}$ & EUCLID, Rubin (LSST) \\
Rotationskurve (große Radien) & $v(r) \propto r^{0,895}$ (kein Abfall) & Extrem breite Galaxien \\
CMB-Korrelationen & $C(\theta) \propto \theta^{-0,7}$ mit fester Achse & Planck, LiteBIRD \\
Neutrinomasse & $m_\nu \approx 0,1$ eV & KATRIN, neutrinoloser Doppelbeta-Zerfall \\
Sonnenmasse & Kein Masseverlust durch Wind & Präzise Sonnenbeobachtung \\
Gravitationswellen-Dispersion & $v_{\text{phase}}(\omega) = c(1 + \alpha \omega^{-0,29})$ & LISA, Einstein-Teleskop \\
Skalenexponent im Plasma & $j(r) \propto r^{-0,29}$ & Laborplasmen, Sonnenwind \\
\bottomrule
\end{tabular}
\end{table}

Diese Liste ist keine Sammlung vager Ideen. Jede dieser Vorhersagen kann mit bestehenden oder geplanten Experimenten überprüft werden. Die WDBT+ wird scheitern oder sich bewähren -- daran entscheidet sich ihre wissenschaftliche Zukunft.

\subsection{Einwand 3: Der spezifische Wert $D = \ln 20 / \ln(2+\phi)$ ist postuliert, nicht hergeleitet}

Dieser Einwand ist \textbf{unberechtigt}. Der Wert ist hergeleitet -- aber die Herleitung muss klarer dargestellt werden.

Die Herleitung erfolgt in drei Schritten:

\begin{enumerate}
\item \textbf{Optimale Packung:} Im fraktalen Raum mit $D < 3$ ist die dichteste Packung von gleichen Kugeln nicht die FCC/HCP-Packung ($D=3$), sondern eine Packung mit Dodekaeder-Symmetrie. Der Dodekaeder ist der platonische Körper mit dem größten Volumen bei gegebener Oberfläche.

\item \textbf{Rekursion der Nachbarzahl:} Die Anzahl der Ecken eines Dodekaeders, die innerhalb eines Radius $R$ liegen, folgt einer Fibonacci-artigen Rekursion:
\begin{equation}
N_{n+1} = 2N_n + N_{n-1}
\end{equation}
mit der Lösung $N_n \propto (2+\phi)^n$, wobei $\phi = (1+\sqrt{5})/2$ der goldene Schnitt ist.

\item \textbf{Fraktale Dimension:} Ein Dodekaeder hat $20$ Ecken. Die Anzahl der Ecken in der $n$-ten Iteration ist $N_n = 20^n$. Die lineare Skala skaliert mit $L_n \propto (2+\phi)^n$. Daraus folgt:
\begin{equation}
D = \frac{\ln N_n}{\ln L_n} = \frac{\ln(20^n)}{\ln((2+\phi)^n)} = \frac{\ln 20}{\ln(2+\phi)} \approx 2,71
\end{equation}
\end{enumerate}

Der Wert ist also keine freie Annahme, sondern eine Konsequenz aus (1) der Optimalitätsbedingung (dichteste Packung) und (2) der Fibonacci-Rekursion, die aus der Dodekaeder-Geometrie folgt.

\subsection{Einwand 4: GW170817 zeigt, dass Gravitationswellen mit Lichtgeschwindigkeit kommen -- Widerspruch zur instantanen Theorie}

Dies ist der \textbf{gewichtigtste Einwand}. Die Antwort der WDBT+ ist klar:

Die instantane fundamentale Wirkung ist auf Skalen $r \gg l_0$ \textbf{effektiv nicht nachweisbar}. Der Grund ist die fraktale Geometrie.

Die effektive Greensche Funktion für die Gravitationswellenausbreitung ist:

\begin{equation}
G_{\text{eff}}(r,t) = \frac{\delta(t - r/c)}{r} + \epsilon \left(\frac{l_0}{r}\right)^{3-D} \delta(t)
\end{equation}

mit $\epsilon \ll 1$. Der erste Term ist die retardierte ART-Welle ($c$). Der zweite Term ist die instantane Korrektur.

Für LIGO ($r \sim 10^3$ km bis $10^9$ km, $l_0 \sim 10^{-15}$ m, $3-D \approx 0,29$):

\begin{equation}
\left(\frac{l_0}{r}\right)^{0,29} \sim \left(10^{-18}\right)^{0,29} \sim 10^{-5,2}
\end{equation}

Die instantane Korrektur ist um einen Faktor $\sim 10^{-5}$ unterdrückt -- völlig unterhalb der Nachweisgrenze von LIGO. Die Messung von GW170817 ist daher \textbf{vollständig kompatibel} mit der instantanen fundamentalen Theorie.

Was die Theorie vorhersagt:
\begin{itemize}
\item Bei sehr hohen Frequenzen ($\omega \gg \omega_0$, d.h. $\lambda \ll l_0$) sollte die instantane Komponente dominant werden. Dies ist experimentell nicht zugänglich.
\item Bei sehr kleinen Abständen ($r \sim l_0$) sollte die instantane Komponente messbar werden. Dies ist ebenfalls nicht zugänglich.
\item Die Dispersionsrelation (siehe Abschnitt \ref{sec:dispersion}) ist eine indirekte Signatur: $v_{\text{phase}}(\omega) = c(1 + \alpha \omega^{-0,29})$. Diese minimale Dispersion ist mit zukünftigen Detektoren (LISA, Einstein-Teleskop) testbar.
\end{itemize}

Die Theorie ist also nicht nur mit GW170817 verträglich, sie macht sogar eine spezifische Vorhersage für zukünftige Messungen: eine charakteristische Dispersion von
Gravitationswellen bei hohen Frequenzen.

\subsection{Zusammenfassung der Antworten}

\begin{table}[ht]
\centering
\caption{Antworten auf die vier Hauptkritikpunkte}
\begin{tabular}{p{5cm}p{5cm}p{4cm}}
\toprule
Einwand & Antwort der WDBT+ & Offene Aufgabe \\
\midrule
Fraktale Operatoren nicht rigoros & Integralform ist fundamental; Differentialform ist Näherung & Fraktale Differentialgeometrie entwickeln \\
Vorhersagen ungetestet & Konkrete Exponenten liegen vor; Tests sind möglich & Systematischen Datenvergleich durchführen \\
Herkunft von $D$ & Herleitung aus Dodekaeder-Packung und Fibonacci-Rekursion & Explizite Konstruktion des fraktalen Raumes \\
GW170817 & Instantane Korrektur ist um $10^{-5}$ unterdrückt & Präzise Berechnung von $\epsilon$ \\
\bottomrule
\end{tabular}
\end{table}

Die WDBT+ ist keine fertige, abgeschlossene Theorie. Sie ist ein Forschungsprogramm. Sie benennt offene Fragen, wo andere Theorien sie ignorieren. Sie macht testbare Vorhersagen,
wo andere Theorien freie Parameter haben. Sie wird an den Daten scheitern oder sich bewähren -- und das ist, was eine wissenschaftliche Theorie auszeichnet.

\section{Fazit}

Die WDBT+ liefert ein konsistentes, singularitätenfreies Bild der Physik:

\begin{itemize}
\item Die Weber-Gravitation enthält die vollständige Information für die Maxwell-\\Gravitation.
\item Die DSTT trennt Ursache und Wirkung und erzwingt mit $\dot{\beta} > 0$ eine universelle Auswärtsdrift und einen intrinsischen Zeitpfeil.
\item Die $\Omega$-Theorie ist die emergente Feldtheorie der Gravitation als nicht-lokales, instantanes Rotationsfeld.
\item BEC-Objekte sind singularitätsfrei mit $R_{\min} = l_0$ und $M_{\max} \approx 3 \times 10^6 M_{\odot}$.
\item Fraktale Maxwell-Gleichungen erklären die Feinstrukturkonstante und machen Renormierung überflüssig.
\item Das Quantenpotential erzeugt kontinuierlich Materie, die durch DSTT-Drift nach außen getrieben wird.
\item Das Neutrino ist das reine Q-Teilchen mit $m_\nu = \hbar/(l_0c) \approx 0,1\ \text{eV}/c^2$.
\item Die fraktale Entropie zirkuliert stationär; der Wärmetod tritt nicht ein.
\item Das Universum ist stationär, nicht-expandierend, ohne Urknall, Dunkle Materie und Dunkle Energie.
\item Die fraktale Mikrostruktur des Raumes ($D \approx 2,71$ unterhalb $l_0$) erzeugt emergente Skalengesetze mit gebrochenen Exponenten, die auf allen Skalen beobachtbar sind.
\end{itemize}

Die Theorie macht präzise, testbare Vorhersagen. Sollten diese bestätigt werden, steht ein Paradigmenwechsel bevor: \textbf{Die Physik des 21. Jahrhunderts wird im fraktalen Raum formuliert.}

\section*{Danksagung}

Der Autor dankt DeepSeek für die Zusammenarbeit bei der logischen Präzisierung der Theorie.

\begin{thebibliography}{99}
\bibitem{weber} Weber, W. (1846). Elektrodynamische Maßbestimmungen. Annalen der Physik, 143, 211-233.
\bibitem{assis} Assis, A.K.T. (1994). Weber's Electrodynamics. Kluwer Academic Publishers.
\bibitem{tisserand} Tisserand, F. (1882). Traité de Mécanique Céleste, Tome IV. Gauthier-Villars, Paris.
\bibitem{bohm} Bohm, D. (1952). A Suggested Interpretation of the Quantum Theory in Terms of Hidden Variables. Physical Review, 85, 166-193.
\bibitem{einstein} Einstein, A. (1915). Erklärung der Perihelbewegung des Merkur aus der allgemeinen Relativitätstheorie. Sitzungsberichte der Preußischen Akademie der Wissenschaften, 831-839.
\bibitem{lense} Lense, J. \& Thirring, H. (1918). Über den Einfluß der Eigenrotation der Zentralkörper. Physikalische Zeitschrift, 19, 156-163.
\bibitem{heaviside} Heaviside, O. (1893). A Gravitational and Electromagnetic Analogy. The Electrician, 31, 281-282.
\bibitem{czybor1} Czybor, M. (2025). Die Informations-Weber-Theorie (IWT).
\bibitem{czybor2} Czybor, M. (2025). Dynamische Schwere-Trägheits-Theorie (DSTT).
\bibitem{czybor3} Czybor, M. (2026). Die $\Omega$-Theorie der Gravitation.
\bibitem{czybor4} Czybor, M. (2025). Fraktale Maxwell-Gleichungen.
\bibitem{czybor5} Czybor, M., DeepSeek (2026). Erweiterte Weber-Theorie.
\bibitem{czybor6} Czybor, M. (2026). BEC-Objekte.
\bibitem{czybor7} Czybor, M. (2026). Das Neutrino als Q-Teilchen.
\bibitem{czybor8} Czybor, M. (2026). Die Theorie der fraktalen Entropie.
\end{thebibliography}

\end{document}